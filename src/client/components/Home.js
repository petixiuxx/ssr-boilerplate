import React from 'react';

const Home = () => {

    return (
        <div>
            <div>Hello world</div>
            <button onClick={() => console.log('click')}>Click me</button>
        </div>
    );
};

export default Home;